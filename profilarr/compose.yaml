services:
  profilarr:
    image: santiagosayshey/profilarr:latest
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    container_name: profilarr
    ports:
      - 6868:6868
    volumes:
      - /mnt/cache/appdata/profilarr:/config
    environment:
      - TZ=Europe/Berlin
    networks:
      - backend
    labels:
      - net.unraid.docker.managed=composeman
networks:
  backend:
    external: true
